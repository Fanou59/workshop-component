export default function Home() {
  return (
    <main>
      <header>Todo</header>
      {/* Création d'un composant Header pour afficher titre de l'app et le logo React 
      -> création d'un Button (et utiliser CVA) pour les 2 boutons du header
      -> les icones provenant de lucid-react sont ShoppingBasket et User
      
      */}
    </main>
  );
}
